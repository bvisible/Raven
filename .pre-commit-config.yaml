exclude: "node_modules|.git"
default_stages: [pre-commit]
fail_fast: false

repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.3.0
    hooks:
      - id: trailing-whitespace
        files: "raven.*"
        # //// Neoffice - commit-the-build: raven/public/raven/** is the committed vite output.
        # //// Rewriting minified bundles after the build makes the committed files differ from
        # //// what vite produced, and v3 ships a service worker whose precache manifest records a
        # //// revision per asset. Never let a formatting hook touch build output.
        exclude: ".*json$|.*txt$|.*csv|.*md|.*svg|^raven/public/raven/"
      - id: check-yaml
      - id: check-merge-conflict
      - id: check-ast
      - id: check-json
        files: "raven.*"
      - id: check-toml
      - id: debug-statements

  - repo: https://github.com/asottile/pyupgrade
    rev: v2.34.0
    hooks:
      - id: pyupgrade
        files: "raven.*"
        args: ["--py310-plus"]

  - repo: https://github.com/adityahase/black
    rev: 9cb0a69f4d0030cdf687eddf314468b39ed54119
    hooks:
      - id: black
        files: "raven.*"
        additional_dependencies: ["click==8.0.4"]

  - repo: https://github.com/PyCQA/flake8
    rev: 6.0.0
    hooks:
      - id: flake8
        files: "raven.*"
        additional_dependencies: ["flake8-bugbear", "flake8-tuple"]
        args: ["--config", ".github/helper/flake8.conf"]

  - repo: https://github.com/PyCQA/isort
    rev: 5.12.0
    hooks:
      - id: isort
        files: "raven.*"
        exclude: ".*setup.py$"

ci:
  autoupdate_schedule: weekly
  skip: []
  submodules: false
