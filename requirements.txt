# //// Neoffice - added file (no upstream equivalent). Upstream declares its dependencies in
# //// pyproject.toml only; bench's app installer still reads requirements.txt on the fleet, so the
# //// same list is mirrored here (markitdown pin + lmstudio included).
# frappe -- https://github.com/frappe/frappe is installed via 'bench init'
linkpreview
openai>=1.100.2
blurhash-python
openai-agents>=0.0.16
markitdown==0.0.2  # Pinned to 0.0.2 to avoid magika/TensorFlow dependency (introduced in 0.1.0)
pandas
google-cloud-documentai
lmstudio>=1.4.0